# send for a prompt that does not exist returns ExitPrompt (3).

mkdir prompts
! tprompt send missing --config config.toml --target-pane %1
stderr 'prompt "missing" not found'

-- config.toml --
prompts_dir = "prompts"
