# Malformed reserved key should fail with exit 2.

mkdir prompts
! tprompt list --config config.toml
stderr 'reserved_keys'
stderr 'tprompt - error:'

-- config.toml --
prompts_dir = "prompts"
default_mode = "paste"

[reserved_keys]
clipboard = "ctrl+x"
