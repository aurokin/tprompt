# `tprompt tui --target-pane %0` with the Phase 5a cancel-stub Renderer exits 0
# silently and draws nothing.

mkdir prompts
tprompt tui --target-pane %0 --config config.toml
! stdout .
! stderr .

-- config.toml --
prompts_dir = "prompts"
